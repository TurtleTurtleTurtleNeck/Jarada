; PlatformIO project configuration — PLACEHOLDER
; Jarada vest firmware. Fill in once hardware bring-up starts.
; Docs: https://docs.platformio.org/page/projectconf.html

[platformio]
default_envs = esp32-proto

; ── Prototype: ESP32 (BLE built in) ──
[env:esp32-proto]
platform = espressif32      ; TODO: pin a version
board = esp32dev            ; TODO: set actual board
framework = arduino
monitor_speed = 115200
; lib_deps =
;   adafruit/Adafruit BNO08x   ; BNO085 driver — TODO: pin version

; ── Production roadmap: nRF52840 ──
; [env:nrf52840-prod]
; platform = nordicnrf52
; board = ...
; framework = arduino
